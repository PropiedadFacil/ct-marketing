<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Club de Descuentos" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Gestor de giftcards y descuentos de Club Tacones" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
  <%= render 'layouts/messages' %>
   <div class="page-wrapper">

   <% if current_page?(root_path) %>
  <%#= render 'layouts/header8' %>
  <% else %>
  <%= render 'layouts/header8' %>
  <% end %>

     <%= yield %>
    <footer class="footer-3">
    <%= render 'layouts/footer' %>
    </footer>
   </div>
  </body>
</html>
